<%= form_for app, :url => "/admin/applicants/#{app.id}/savereject" do |form| %>

    	<%= form.label :notes %><br/>
        <%= form.text_area :notes %><br/>
        
        <%= form.submit :value => 'Reject' %>

<% end %>
